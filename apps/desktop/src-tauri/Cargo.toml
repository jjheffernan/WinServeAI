# Tauri app crate — enabled when Phase 2 begins.
# Add "apps/desktop/src-tauri" to the workspace members list at that time.

[package]
name = "winserve-desktop"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "WinServeAI desktop shell (Tauri)"
publish = false

[dependencies]
# winserve-launcher = { path = "../../../packages/launcher" }
# tauri = { version = "2", features = [] }
