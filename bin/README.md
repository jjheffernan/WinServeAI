# bin/

Place the pinned `llama-server` binary here:

```text
bin/llama-server.exe   # Windows
```

This is the **external system**. WinServeAI never embeds llama.cpp source; it only spawns this executable.

Ship MIT notices for llama.cpp with the installer (`THIRD_PARTY_NOTICES`).
