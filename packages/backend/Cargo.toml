[package]
name = "winserve-backend"
description = "Backend abstraction layer — UI never talks to llama.cpp directly"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
winserve-shared = { workspace = true }
async-trait = { workspace = true }
serde = { workspace = true }
