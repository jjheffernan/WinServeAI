[package]
name = "winserve-hardware"
description = "GPU, VRAM, CUDA, CPU, and RAM detection (Windows-first)"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
winserve-shared = { workspace = true }
serde = { workspace = true }
